import React, { useState } from "react";

import ProfileUpperNavigationBar from "./ProfileUpperNavigationBar";
import ProfileSidebar from "./ProfileSideBar";
import SentRequestTable from "./SentRequestTable";
import ReceivedRequestTable from "./ReceivedRequestTable";

const ProfilePage = () => {
  const [userName] = useState("Geetanshi Jain");
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [activeTab, setActiveTab] = useState("sent");

  return (
    <>
      <ProfileUpperNavigationBar />
      
      {/* Sidebar */}
      <ProfileSidebar
        sidebarOpen={sidebarOpen}
        setSidebarOpen={setSidebarOpen}
        userName={userName}
      />

      {/* Main Content */}
      <div className="min-h-screen bg-gradient-to-br from-blue-100 to-white p-8">
        <div className="max-w-7xl mx-auto grid grid-cols-12 gap-6 mt-20 ml-70  ">

          {/* Greeting Section */}
          <div className="col-span-12 bg-white shadow-lg rounded-2xl p-6">
            <h2 className="text-2xl font-semibold text-gray-800">
              👋 Hello, {userName}!
            </h2>
            <p className="text-gray-600 mt-1">
              Welcome back to your Smart Farming Dashboard.
            </p>
          </div>

          {/* Labour Profile */}
          <div className="col-span-12 md:col-span-4 bg-white shadow-md rounded-xl p-5 hover:shadow-xl transition">
            <h3 className="text-lg font-semibold text-gray-800">👷‍♀ Labour Profile</h3>
            <p className="text-sm text-gray-500 mt-2">Manage your labour profiles</p>
          </div>

          {/* Your Machinery */}
          <div className="col-span-12 md:col-span-4 bg-white shadow-md rounded-xl p-5 hover:shadow-xl transition">
            <h3 className="text-lg font-semibold text-gray-800">🚜 Your Machinery</h3>
            <p className="text-sm text-gray-500 mt-2">Check and track your equipment</p>
          </div>

          {/* Your Land */}
          <div className="col-span-12 md:col-span-4 bg-white shadow-md rounded-xl p-5 hover:shadow-xl transition  ">
            <h3 className="text-lg font-semibold text-gray-800">🌾 Your Land</h3>
            <p className="text-sm text-gray-500 mt-2">View and update your land records</p>
          </div>

          {/* Your Requests Section */}
          <div className="col-span-12 lg:col-span-12 bg-white shadow-md rounded-2xl p-6">
            <h3 className="text-xl font-bold text-gray-800 mb-6">📩 Your Requests</h3>
            <div className="flex gap-4 mb-4">
              <button
                onClick={() => setActiveTab("sent")}
                className={`px-4 py-2 rounded ${
                  activeTab === "sent"
                    ? "bg-blue-600 text-black"
                    : "bg-white border border-black"
                }`}
              >
                Sent Request
              </button>

              <button
                onClick={() => setActiveTab("received")}
                className={`px-4 py-2 rounded ${
                  activeTab === "received"
                    ? "bg-blue-600 text-white"
                    : "bg-white border border-black"
                }`}
              >
                Received Request
              </button>

            </div>

        
          </div>
          <div className="col-span-12 lg:col-span-12 bg-white shadow-md rounded-2xl p-6" >
        
              {/* Conditional Table Render */}
              {activeTab === "sent" ? <SentRequestTable /> : <ReceivedRequestTable />}
              </div>

          {/* Right Side – Bookings & Transactions */}
          <div className="col-span-12 lg:col-span-8 bg-white shadow-md rounded-2xl p-6">
            <div className="bg-white shadow-md rounded-2xl p-5">
              <h4 className="text-lg font-semibold text-gray-800">📅 Your Bookings</h4>
              <p className="text-sm text-gray-500 mt-2">Check your upcoming activities</p>
            </div>

            <div className="bg-white shadow-md rounded-2xl p-5">
              <h4 className="text-lg font-semibold text-gray-800">💰 Your Transactions</h4>
              <p className="text-sm text-gray-500 mt-2">Track your payment history</p>
            </div>
          </div>
        </div>
      </div>
    </>
  );
};

export default ProfilePage;